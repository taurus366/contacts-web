import { EmptyFieldDirective } from './empty-field.directive';

describe('EmptyFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyFieldDirective();
    expect(directive).toBeTruthy();
  });
});
